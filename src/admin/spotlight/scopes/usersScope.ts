/**
 * Users scope — list and search users (Phase 3).
 */

import type { Scope } from '../types'

export const usersScope: Scope = {
  id: 'users',
  title: 'Open user',
  placeholder: 'Search users…',
  commands: () => [],  // Phase 2/3
}
